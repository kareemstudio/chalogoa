{
  "name": "Chalo Goa",
  "install": "test -f index.html && test -f app.js && test -f songs.js && test -f data.js && test -f styles.css",
  "terminals": [
    {
      "name": "web",
      "command": "python3 -m http.server 8000 --bind 0.0.0.0",
      "description": "Static site preview server (see README)"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 8000
    }
  ]
}
